package stratego;

/**
 * These are the possible results of the combat.
 */
public enum CombatResult {
    /**
     * This is the win result of combat.
     */
    WIN,
    /**
     * This is the draw result of combat.
     */
    DRAW,
    /**
     * This is the lose result of combat.
     */
    LOSE;
}
